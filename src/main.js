import './style.css'

const app = document.getElementById('app')
app.innerHTML = `
  <main>
    <h1>StyleSync — Vite App</h1>
    <p>Welcome. Run <code>npm run dev</code> to start Vite.</p>
  </main>
`
